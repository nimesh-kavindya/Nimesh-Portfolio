{
  "name": "nimesh-portfolio",
  "version": "1.0.0",
  "description": "Portfolio website for Nimesh Kavindya",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx tailwindcss -i input.css -o output.css --minify && node server.js",
    "start": "node server.js",
    "build": "npx tailwindcss -i input.css -o output.css --minify",
    "lint": "node -c server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
